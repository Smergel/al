<div class="container-fluid">
	<div class="row top">
		
	</div>
</div>
<div class="container">
	<div class="row form_space">
		<div class="col-lg-6">
			<table style="width:100%">
				<tr>
					<th></th>
					<th>Songlist</th>
					<th>Score</th>
				</tr>
				<% @leaderboard.sort_by(&:score).reverse.each do |x| %>
				<tr>
					<td class="round"><%= image_tag User.find(x.user_id).profile.avatar.url(:thumb) %></td>
					<td><%= "Album Exchange" %></td>
					<td><%= x.score %></td>
				</tr>
				<% end %>
			</table> 
		</div>
	</div>
</div>