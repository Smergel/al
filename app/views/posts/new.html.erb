<div class="container-fluid">
	<div class="row top">
		
	</div>
</div>
<div class="container">
	<div class="row form_space">
		<div class="col-lg-6">
			<%= render "new_post" %>
		</div>
		<div class="col-lg-6 doc">
			<legend>Documentation</legend>
			<h4>Your Next 5 Dates - Year : Month : Day</h4>
			<% if !current_user.nil? %>
			<ul>
				<% @dates.each do |x| %>
					<li><%= x %></li>
				<% end %>
			</ul><% end %><hr />
			<h4>Quote</h4>
			<p>Do not put " " marks around your quote.</p><hr />
			<h4>URLs</h4>
			<p>Make sure to copy the entire link (it needs to start with http://). The picture links must end with .jpg, .jpeg, or .png.</p><hr />
			<h4>Band Picture</h4>
			<p>Band Pictures must be <strong>rectangular</strong>, however make sure the height is at least 1/2 of the width, and the width must be between <strong>800 pixels</strong> and <strong>2000 pixels</strong>.</p><hr />
			<h4>Album Cover</h4>
			<p>Album Covers must be <strong>square</strong>, with a width between <strong>200 pixels</strong> and <strong>500 pixels</strong>.</p><hr />
		</div>
	</div>
</div>
